DATABASE_URL="file:./dev.db"

# Random secret used to sign session JWTs. Generate your own, e.g. `openssl rand -base64 32`.
AUTH_SECRET=

# Optional: set to enable LLM-assisted requirement analysis and match ranking.
# Falls back to rule-based tag matching when unset.
ANTHROPIC_API_KEY=
